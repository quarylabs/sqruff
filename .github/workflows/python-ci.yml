# This file is autogenerated by maturin v1.7.8
# To update, run
#
#    maturin generate-ci github --manifest-path crates/cli/Cargo.toml --output .github/workflows/python-ci.yaml
#
name: CI

on:
  push:
    branches:
      - main
      - master
    tags:
      - "*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:

  # macos:
  #   runs-on: ${{ matrix.platform.runner }}
  #   strategy:
  #     fail-fast: false # Allow other variants to continue even if one fails
  #     matrix:
  #       platform:
  #         - runner: macos-14
  #           target: aarch64-apple-darwin
  #   steps:
  #     - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # ratchet:actions/checkout@v4
  #     - uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # ratchet:actions/setup-python@v5
  #       with:
  #         python-version: "3.10"
  #     - name: Build wheels
  #       uses: PyO3/maturin-action@36db84001d74475ad1b8e6613557ae4ee2dc3598 # ratchet:PyO3/maturin-action@v1
  #       with:
  #         target: ${{ matrix.platform.target }}
  #         args: --release  --interpreter python3.13 --out dist --manifest-path crates/cli/Cargo.toml --target aarch64-apple-darwin
  #         sccache: "true"
  #     - name: Upload wheels
  #       uses: actions/upload-artifact@4cec3d8aa04e39d1a68397de0c4cd6fb9dce8ec1 # ratchet:actions/upload-artifact@v4
  #       with:
  #         name: wheels-macos-${{ matrix.platform.target }}
  #         path: dist

  linux:
    runs-on: ubuntu-latest
    container:
      image: docker://messense/rust-musl-cross:x86_64-musl
      env:
        CFLAGS_armv7_unknown_linux_musleabihf: '-mfpu=vfpv3-d16'
    steps:
      - uses: actions/checkout@v3
      - name: Build Wheels - manylinux
        uses: PyO3/maturin-action@main
        with:
          target: x86_64-unknown-linux-musl
          manylinux: auto
          container: off
          args: --release  --out dist --manifest-path crates/cli/Cargo.toml
      - name: Build Wheels - musllinux
        uses: PyO3/maturin-action@main
        with:
          target: x86_64-unknown-linux-musl
          manylinux: musllinux_1_1
          container: off
          args: --release -o dist
      - name: Upload wheels
        uses: actions/upload-artifact@v3
        with:
          name: wheels
          path: dist
