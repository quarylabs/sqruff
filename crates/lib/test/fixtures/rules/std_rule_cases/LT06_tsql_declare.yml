rule: LT06

test_fail_tsql_declare_table_variable:
  fail_str: |
    -- Test LT06 with DECLARE TABLE
    SELECT COALESCE (value1, '') FROM table1;
    
    DECLARE @customers TABLE (CustomerId INT);
    
    SELECT COALESCE (value2, '') FROM table2;
    
    SELECT COALESCE (value3, '') FROM table3;
  fix_str: |
    -- Test LT06 with DECLARE TABLE
    SELECT COALESCE(value1, '') FROM table1;
    
    DECLARE @customers TABLE (CustomerId INT);
    
    SELECT COALESCE(value2, '') FROM table2;
    
    SELECT COALESCE(value3, '') FROM table3;
  configs:
    core:
      dialect: tsql