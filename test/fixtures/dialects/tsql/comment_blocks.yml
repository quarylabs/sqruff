# Test metadata for comment_blocks.sql
# Copied from sqlfluff to ensure comment handling compatibility

test_tsql_comment_blocks:
  description: "Nested block comments and edge cases"
  expected_success: true
  
  # These tests ensure we handle:
  # - Nested block comments
  # - Comments with special characters inside
  # - Multi-line block comments