# Test metadata for inline_comments.sql
# Validates that # is NOT treated as inline comment in T-SQL

test_tsql_inline_comments:
  description: "Ensure only -- starts inline comments, not #"
  expected_success: true
  
  # This test validates:
  # - Only -- starts inline comments in T-SQL
  # - # is NOT a comment character
  # - # can appear in identifiers (start or end)
  # - # in strings is just text
  # - # followed by -- comment works correctly